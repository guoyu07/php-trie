// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("trie", "for trie support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("trie", "enable trie support", "no");

if (PHP_TRIE != "no") {
	EXTENSION("trie", "trie.c");
}

